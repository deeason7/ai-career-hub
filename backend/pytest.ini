[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests that call a real LLM (Ollama or Groq)
filterwarnings =
    ignore::DeprecationWarning:sqlmodel.*
