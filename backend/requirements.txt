# --- Core ---
fastapi==0.136.0
uvicorn[standard]==0.30.1
slowapi==0.1.9
sentry-sdk[fastapi]==2.8.0
pydantic>=2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.31
httpx>=0.27.0
beautifulsoup4==4.14.3

# --- Database ---
sqlmodel==0.0.19
alembic==1.18.4
psycopg[binary]==3.2.1
greenlet>=3.4.0

# --- Session / Rate Limiting ---
redis==5.0.7

# --- AI & NLP ---
langchain-community==0.4.2
langchain-core==1.4.8
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
langchain-groq==1.1.3
faiss-cpu==1.14.3
tiktoken==0.7.0
pymupdf==1.24.7
python-docx==1.1.2
# CPU-only torch: the default PyPI build pulls ~3.5 GB of CUDA + Triton wheels
# that this inference-only, CPU-bound app never uses. Pin the CPU wheel instead.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
sentence-transformers==3.0.1
langgraph==1.2.7
langgraph-checkpoint==4.1.1
langgraph-sdk==0.4.2
chromadb>=1.0.0
qdrant-client==1.18.0
ddgs>=9.0.0
# --- pip-audit accepted-risk ignores ---
# PYSEC-2024-278 / CVE-2024-2057 (langchain-community TFIDFRetriever SSRF):
#   False positive — advisory targets <=0.0.26; fixed in 0.0.27. We are on 0.4.2.
#   TFIDFRetriever.load_local() is never called in this codebase.
# PYSEC-2025-183 / CVE-2025-45768 (pyjwt weak key length):
#   Disputed by PyJWT maintainers — key length is the application's responsibility.
#   We use a cryptographically random 32-byte SECRET_KEY from AWS SSM. Not exploitable.
# PYSEC-2025-211..218 (transformers Perceiver/model deserialization RCEs):
#   Require loading an attacker-controlled model file. We only load all-MiniLM-L6-v2
#   from Hugging Face Hub at startup. No user-controlled model paths.
# PYSEC-2025-189..197, PYSEC-2025-210, PYSEC-2026-139 (torch profiler/training/pt2 handler):
#   Affect torch profiler tuple handler, training-only code paths, and pt2 loading handler.
#   PYSEC-2026-139 (CVE-2026-4538) is local-access-only (AV:L) — requires loading a
#   malicious local file. This codebase does inference only; no user-controlled model paths.
# PYSEC-2024-277 / CVE-2024-34997 (joblib NumpyArrayWrapper deserialization):
#   Disputed by joblib maintainers — only triggered when caching trusted content.
#   We do not serialize/deserialize joblib caches from untrusted sources.
# CVE-2026-1839 (transformers Trainer._load_rng_state RCE):
#   Training-only code path. Inference-only usage. Fix (5.0.0rc3) is pre-release.
# CVE-2026-4372 (transformers config-injection RCE via _attn_implementation_internal):
#   Only exploitable with the optional `kernels` package installed AND an
#   attacker-controlled model config.json. We do not depend on `kernels` and only
#   load all-MiniLM-L6-v2 from HF Hub at startup — no user-controlled model paths.
#   Fix (5.3.0) is a major bump incompatible with sentence-transformers==3.0.1.
# CVE-2026-45829 (chromadb):
#   Accepted risk. ChromaDB collections are per-user and server-side only.
#   No user-controlled query paths to the underlying storage layer.
# PYSEC-2026-3447 / CVE-2026-59890 (setuptools MANIFEST.in Unicode-normalization bypass):
#   Build-tooling only — affects sdist creation on macOS APFS/HFS+ filesystems.
#   setuptools is not a declared dependency here; the flagged copy is pip-audit's own
#   resolution environment. We never build sdists, and images build on Linux.
# PYSEC-2026-2290 / CVE-2026-5241 (transformers LightGlue trust_remote_code bypass RCE):
#   Requires loading an attacker-controlled model repo via AutoModel.from_pretrained().
#   We only load all-MiniLM-L6-v2 from HF Hub at startup — no user-controlled model
#   paths. No fixed release exists (all versions through 5.2.0 are affected).
groq>=0.9.0,<1.0.0
instructor>=1.3.0

# --- File Handling ---
python-magic==0.4.27
reportlab==5.0.0

# --- Security ---
bcrypt==4.2.0
PyJWT==2.13.0
cryptography>=49.0.0

