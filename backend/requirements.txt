# --- Core ---
fastapi==0.136.0
uvicorn[standard]==0.30.1
slowapi==0.1.9
sentry-sdk[fastapi]==2.8.0
pydantic>=2.9.0
pydantic-settings==2.4.0
python-multipart==0.0.27
httpx>=0.27.0
beautifulsoup4==4.14.3

# --- Database ---
sqlmodel==0.0.19
alembic==1.13.2
psycopg[binary]==3.2.1
greenlet>=3.4.0

# --- Session / Rate Limiting ---
redis==5.0.7

# --- AI & NLP ---
langchain-community==0.3.27
langchain-core>=0.3.84,<1.0.0
langchain-text-splitters==0.3.9
langchain-groq==0.3.2
faiss-cpu==1.8.0.post1
tiktoken==0.7.0
pymupdf==1.24.7
python-docx==1.1.2
sentence-transformers==3.0.1
# CVE-2026-1839 (transformers transitive dep): RCE in Trainer._load_rng_state() —
# only triggered during model training, not inference. Fix (5.0.0rc3) is pre-release.
# Accepted risk; ignored in pip-audit. Revisit when 5.x stable ships.
# CVE-2026-41481 (langchain-text-splitters SSRF): HTMLHeaderTextSplitter.split_text_from_url()
# not used in this codebase. Fix requires 1.x ecosystem upgrade (blocked by M5.3: groq<1.0.0).
# Accepted risk; ignored in pip-audit. Resolve in chore/langchain-v1-upgrade.
groq>=0.9.0,<1.0.0
instructor>=1.3.0

# --- File Handling ---
python-magic==0.4.27
reportlab==4.5.0

# --- Security ---
bcrypt==4.2.0
PyJWT==2.12.1
cryptography>=44.0.0

