# Development + testing dependencies (not installed in production Docker image).
#
# Usage:
#   pip install -r requirements.dev.txt   <- local dev / CI
#   pip install -r requirements.txt       <- production Docker image

-r requirements.txt

# --- Testing ---
pytest==8.3.5
pytest-asyncio==0.26.0

# --- Linting & Type Checking ---
ruff==0.11.6
mypy==1.15.0

# --- Git Hooks ---
pre-commit==4.2.0
