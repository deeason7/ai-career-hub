# Development + testing dependencies.
# Includes everything from requirements.txt (production) plus dev tooling.
#
# Usage:
#   pip install -r requirements.dev.txt   ← local dev / CI
#   pip install -r requirements.txt       ← production Docker image (Dockerfile)
#
# DO NOT add these to requirements.txt — they inflate the ~3GB production image
# by ~300-400MB and add ~60-90s to every ECR build for zero runtime benefit.

-r requirements.txt

# --- Testing ---
pytest==8.3.2
pytest-asyncio==1.3.0

# --- Linting & Type Checking ---
ruff==0.5.5
mypy==1.11.1

# --- Git Hooks ---
pre-commit==3.7.1
