name: Deploy

on:
  push:
    branches: [main]

# CD is manual for now: SSH into EC2, run infra/scripts/pull-secrets.sh,
# then docker compose -f docker-compose.prod.yml up -d --remove-orphans.
# This workflow will be expanded when GitHub Actions → EC2 SSH is configured.

jobs:
  noop:
    name: CD is manual — see infra/scripts/
    runs-on: ubuntu-latest
    steps:
      - run: echo "No automated CD yet. Deploy manually via EC2 SSH."
